---
use: ["events"]
permalink: events.json
---
{
  "success": 1,
  "result": [
    {% for event in data.events %}
    {
    "id": {{ loop.index0 }},
    "title": "{{ event.meta.title }}",
    "url": "{{ event.meta.link }}",
    "class": "{{ event.meta.type|type_css_class }}",
    "start": "{{ event.meta.date|unixtime }}",
    "end": "{{ event.meta.date|unixtime }}"
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
